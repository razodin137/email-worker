# Email Worker

A Cloudflare Worker for handling email operations.

## Setup

1.  Install dependencies:
    ```bash
    npm install
    ```

2.  Run locally:
    ```bash
    npx wrangler dev
    ```

3.  Deploy:
    ```bash
    npx wrangler deploy
    ```
